data_dir: "/home/jackzhy/dvrk_multimodal_data_collection/data"
data_name: "data_20250807"
data_index: "1"
#data_name: "data_20250808"
#data_index: "6"
#data_name: "data_20250911/suturing"
#data_index: "2"
#data_name: "data_ubc_20250814"
#data_index: "ubc03"
raw_dir: "${.data_dir}/raw/${.data_name}/${.data_index}"
# M1 outputs live per-clip under `<raw_dir>/preprocess/<stage>/` so each
# clip directory is self-contained. `intermediate_dir` = stage-1 bucket
# (rectify_resize); `processed_dir` = parent — siblings are the per-stage
# outputs (kinematic_reproject/, depth_estimation/, optical_flow/,
# kinematic_reproject_drawframe/).
intermediate_dir: "${.raw_dir}/preprocess/rectify_resize"
processed_dir:  "${.raw_dir}/preprocess"